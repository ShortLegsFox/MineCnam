using UnityEngine;

public class EnemyMovement : MonoBehaviour
{
    //private NavMeshAgent agent;

    //void Start()
    //{
    //    agent = GetComponent<NavMeshAgent>();

    //    if (agent == null)
    //        throw new System.Exception("NavMeshAgent not found on " + gameObject.name);

    //    agent.speed = GetComponent<Enemy>().enemyData.MoveSpeed;
    //    SetTarget(GameManager.Instance.Castle);
    //}

    //public void SetTarget(GameObject target)
    //{
    //    if (target != null)
    //        agent.SetDestination(target.transform.position);
    //}


}
